import { Component } from "@angular/core"

export interface Component_type {
    name: String;
    type: String;
    price: number;
    stock: number;
    clearance: boolean;
    quantity: number;
}